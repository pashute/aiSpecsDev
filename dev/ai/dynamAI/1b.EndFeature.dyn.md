## Filename: 1b.EndFeature.dyn.md
**Version:** 0.2.0

# Pseudo:
Goal: End a Gitflow feature branch and merge to develop

Inputs: None (uses current feature from projmng.yaml)

Prerequisites:
- All items in feature are closed
- Feature branch is clean (committed and pushed)
- Developer confirmation before closing feature
- On problems NOTIFY, HALT, and CONSULT

1. Validation:
1.1. Verify all items in feature are closed
1.2. Verify feature branch is clean
1.3. Verify all commits are pushed
1.4. Verify on feature branch

2. Action steps:
2.1. Switch to develop branch
2.2. Merge feature branch to develop
2.3. Push develop branch
2.4. Delete feature branch (local and remote)
2.5. Update projmng.yaml (clear feature, active_item, parent_item)
2.6. Clear steps.md

3. Expected Results:
- Feature merged to develop
- Feature branch deleted
- projmng.yaml cleared
- steps.md cleared

# General instructions for this dynamAI task:
Reminders:
- See ai/instructions.md section 2.2. Specifically:
  - Discuss with the developer includes waiting for their response.
  - Stop execution if something is wrong.
  - On error NOTIFY, HALT, and CONSULT according to `halt.dyn.md`

- See formats/format.steps.md for instructions about writing to steps.md file

- See formats/format.dynamAI.md for instructions to close a step
(steps.md, notify developer)

# Steps:

## Input Validation steps

### Step 1.0 Record self
1.0.1 **Record dynamAI task:** write to steps.md:
`## Running dynamAI task: 1b.EndFeature by AI assistant (Cascade)`
No result checking here. Continue.

1.0.2 Write project info to steps.md:
`Project info: {projman.name}, {feature.branch}, [{feature.item.num}.{feature.item.title}]({feature.item.url})`
No result checking here. Continue.

### Step 1.1 First confirmation (feature end)
See dynamAI/confirmation.md for CRITICAL HALT INSTRUCTION and confirmation format.

1.1.1 **Display confirmation question** (see format.dynamAI.md "Confirmation question format"):
  - {accomplished}: "All items closed"
  - {assurance}: "Will reconfirm after accomplishment details"
  - {question}: "Proceed with ending feature?"
1.1.2 **Follow confirmation.md CRITICAL HALT INSTRUCTION** - wait for explicit user approval
On result: - follow developer's instructions

### Step 1.2 Verify all items closed
1.2.1 Check completed_items in projmng.yaml
1.2.2 Verify feature.item has no open subitems using aiCode/subItemsClosed.md
1.2.3 If open items exist, warn developer and ask to close them first
On result:
- problem: Consult the developer
- no problem: Close step

### Step 1.3 Critical: Verify feature branch clean
1.3.1 Check working tree is clean: `git status --short`
1.3.2 If uncommitted files exist:
  - If only steps.md and projmng.yaml are uncommitted:
    - Check if they reflect a recent completed commit (compare with projmng.yaml commits section)
    - If yes: Notify developer that metadata files are uncommitted but reflect recent commit
    - Consult developer: Should we add a step to commit metadata files only, then re-run dynamAI task?
    - Wait for developer's ok
    - If developer agrees: Add step to steps.md to commit metadata, stop dynamAI task
  - If other files are uncommitted: WARN developer and ask to resolve first
1.3.3 Check everything is pushed: `git status -sb`
1.3.4 If not pushed, warn developer and ask to resolve
On result:
- problem: Halt the task. Consult the developer
- no problem: Close step

### Step 1.4 Critical: Verify on feature branch
1.4.1 Get current local branch: `git branch --show-current`
1.4.2 If not on feature branch, warn developer and ask to switch
On result:
- problem: Halt task. Consult the developer
- no problem: Close step


### Step 1.5 Show summary and confirm actions
1.5.1 Display feature details from projmng.yaml (feature name, branch, item)
1.5.2 Display completed_items list from projmng.yaml
1.5.3 Display summary of actions to be taken:
  - Switch to develop branch
  - Merge feature branch to develop
  - Push develop branch
  - Delete feature branch (local and remote)
  - Clear projmng.yaml and steps.md
1.5.4 **Second confirmation** - See dynamAI/confirmation.md for CRITICAL HALT INSTRUCTION
1.5.5 **Display confirmation question** (see format.dynamAI.md "Confirmation question format"):
  - {accomplished}: "Ready to end feature"
  - {assurance}: "Actions listed above"
  - {question}: "Proceed with merge and cleanup?"
1.5.6 **Follow confirmation.md CRITICAL HALT INSTRUCTION** - wait for explicit user approval
On result:
- problem: Consult the developer
- no problem: Close step

## Action steps

### Step 2.1 Switch to develop branch
2.1.1 Checkout develop branch: `git checkout develop`
2.1.2 Verify: `git branch --show-current`
2.1.3 Pull latest develop: `git pull`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.2 Merge feature to develop
2.2.1 Merge feature branch: `git merge {feature.branch}`
2.2.2 Resolve conflicts if any (consult developer)
2.2.3 Push develop branch: `git push origin develop`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.3 Delete feature branch
2.3.1 Delete local feature branch: `git branch -d {feature.branch}`
2.3.2 Delete remote feature branch: `git push origin --delete {feature.branch}`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.4 Update projmng.yaml
2.4.1 Clear feature section
2.4.2 Clear active_item section
2.4.3 Clear parent_item section
2.4.4 Clear commits section
2.4.5 Clear completed_items section
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.5 Clear steps.md
2.5.1 Clear all steps
2.5.2 Leave only header and reader lines
On result:
- problem: Consult the developer
- no problem: Close step
