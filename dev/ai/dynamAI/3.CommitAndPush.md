## Filename: 3.CommitAndPush.md
**Version:** 0.2.1

# Pseudo:
Goal: Commit and push changes with proper tracking

Inputs: None (uses active_item from projdev.yaml)

Prerequisites:
- Developer confirmation before commit/push
- Only files relevant to active item should be committed
- Out-of-scope files require explicit approval

0. On problems: **HALT and consult developer**. See instructions.md section 2.2:
   - 2.2.2 Consult means wait for developer instruction during back-and-forth discussion
   - 2.2.6 Halt on errors: report and stop if anything is missing or goes wrong
   - 2.2.7 Stick to dynamAI sequence: if a problem occurred, don't continue even if workaround seems available
   - Continue only after developer releases AI to proceed to next stop point

1. Validation:
1.1. Verify developer meant to commit and push
1.2. Verify all steps to be committed are relevant to active_item
1.3. List out-of-scope files and get explicit approval

  Steps:
    {Self listing}
    {Validation steps}
    {Action steps}
    {Result verification steps}

2. Action steps:
2.1. Create completion table (item steps vs steps.md)
2.2. Create commit message according to template
2.3. Show summary to developer
2.4. Phase 1: Commit all files (work + metadata with completed steps listed)
2.5. Phase 1: Push to origin
2.6. Verify phase 1 commit and push succeeded
2.7. Update projdev.yaml commits section
2.8. Delete completed steps from steps.md
2.9. Phase 2: Commit metadata changes only (steps.md, projdev.yaml)
2.10. Phase 2: Push to origin
2.11. Verify phase 2 commit and push succeeded
2.12. Add comment to GitHub project item

3. Expected Results:
- Changes committed locally
- Changes pushed to origin
- projdev.yaml updated with commit details
- GitHub project item commented with commit link
- steps.md cleaned up 

# General instructions for this dynamAI task:
Reminders:
- See ai/instructions.md section 2.2. Specifically:
  - Discuss with the developer includes waiting for their response.
  - Always discuss problems with the developer.
  - Stop execution if something is wrong.
- See formats/format.steps.md for instructions about writing to steps.md file
- Close step means:
  - Mark step done in `steps.md`
  - Notify developer with telegraphic summary of accomplishment
  - Continue to next step. No need for developer's ok.
- Before any PowerShell command or web access: announce in AI chat what you are doing and why
- After any PowerShell command or web access: report in AI chat what was accomplished

# Steps:

## Input Validation steps

### Step 1.0 Record self
1.0.1 **Record dynamAI task:** write to steps.md:
`## Running dynamAI task: 3.CommitAndPush`
No result checking here. Continue.

1.0.2 Write project info to steps.md:
`Project info: {projman.name}, {feature.branch}, [{active_item.num}.{active_item.title}]({active_item.url})`
No result checking here. Continue.

### Step 1.1 Verify request
This is a stop point.
For the AI: STOP! NO COMMIT WITHOUT IMMEDIATE DEVELOPER'S CONSENT.   
(The previous line should not be shown to the developer it is for the AI eyes only) pleaser remember to stop here. 

1.1.1 **Display confirmation question** (see format.dynamAI.md "Confirmation question format"):
  - {accomplished}: "All changes ready"
  - {assurance}: "Will reconfirm after accomplishment details"
  - {question}: "Commit only or commit and push?"

1.1.2 **Wait for developer's ok to proceed**
On result:
- According to user's response


### Step 1.2 Create completion table
1.2.1 Read item's steps from description and comments (checkbox steps from GitHub issue)
1.2.2 For each item step, find similar steps in steps.md and list them
1.2.3 Decide if step was completed or not by marking it with [v?] (completed) or [x?] (not completed)
1.2.4 Create completion table showing:
  - Item step from GitHub (original wording)
  - Corresponding steps.md step (if found)
  - Completion status ([v?] or [x?])
1.2.5 If no steps found in item description/comments, note that and proceed
1.2.6 When done all steps, consult with user and WAIT FOR OK TO PROCEED
On result:
- problem: Consult the developer
- no problem: Close step

## Action steps

### Step 2.1 Create commit message
2.1.1 Get the commit hash (will be generated after commit)
2.1.2 Give the commit a headline
2.1.3 Give short details
2.1.4 List files (path) and changes
2.1.5 Include itemsteps in commit message (see format.commit.md)
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.2 Show summary to developer
2.2.1 Display completion table
2.2.2 Display commit message (headline + details first - WHAT was done)
2.2.3 Display file list secondary (5 files per row max)
2.2.4 **Display confirmation question** (see format.dynamAI.md "Confirmation question format"):
  - STOP!! NO COMMIT WITHOUT AN INFORMED DEVELOPER'S CONSENT
  - {accomplished}: "Ready to commit"
  - {assurance}: "Details listed above"
  - {question}: "Proceed with commit and push?"
2.2.5 Wait for developer's ok to proceed
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.3 Phase 1: Commit all files
2.3.1 Commit all changes (work files + metadata with completed steps listed): `git commit -m "{commit message}"`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.4 Phase 1: Push to origin
2.4.1 Push to origin: `git push origin {feature.branch}`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.5 Verify phase 1 commit and push
2.5.1 Verify success of commit: `git log -1 --format="%H %s"`
2.5.2 Get commit URL with: `git log -1 --format="%H"` then construct URL: https://github.com/{owner}/{repo}/commit/{hash}
2.5.3 Verify success of push: `git log origin/{branch}..HEAD` (should be empty if pushed)
2.5.4 Warn and stop if anything goes wrong
2.5.5 Notify if succeeded with commit hash and GitHub URL
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.6 Update projdev.yaml
2.6.1 Update projdev.yaml commits section with commit details (see projdevUpdate.md "On Commit and Push" section for format)
2.6.2 In projdev.yaml completed section, only include items done or deferred at this stage
2.6.3 Delete completed steps from steps.md up to the commit point
2.6.4 If next steps are unclear without context, add telegraphic summary of previous steps
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.7 Phase 2: Commit metadata changes
2.7.1 Stage metadata files only: `git add steps.md dev/ai/working/projdev.yaml`
2.7.2 Commit metadata: `git commit -m "Update project tracking metadata"`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.8 Phase 2: Push to origin
2.8.1 Push to origin: `git push origin {feature.branch}`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.9 Verify phase 2 commit and push
2.9.1 Verify success of commit: `git log -1 --format="%H %s"`
2.9.2 Verify success of push: `git log origin/{branch}..HEAD` (should be empty if pushed)
2.9.3 Warn and stop if anything goes wrong
2.9.4 Notify if succeeded
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.10 Add comment to project item
2.10.1 Add comment to the GitHub project item (see format.dynamAI.md "GitHub issue comment format"):
  - {commit_hash}: Phase 1 commit hash
  - {commit_url}: Full GitHub commit URL
  - {telegraphic_summary}: Brief summary of changes
  - {question}: "Commit only or commit and push?" (already answered, use same response)
2.10.2 Use aiCode/itemComment.md to add comment
2.10.3 Input: owner, repo, itemNum, comment
2.10.4 Retrieve the comment URL from the response
2.10.5 Show the developer the link to the created comment
On result:
- problem: Consult the developer
- no problem: Close step

# Commit message template
See /dev/ai/dynamAI/formats/format.commit.md for format details



