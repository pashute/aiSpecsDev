## Filename: 1a.StartFeature.dyn.md
**Version:** 0.1.8.1

# Pseudo:
Goal: Start a new Gitflow feature branch from develop branch

Inputs: feature name (optional), GitHub issue URL/number (optional)

Prerequisites:
- Must be on develop branch with clean committed and pushed state
- Feature branch must not already exist locally or on remote
- Developer confirmation before creating branch
- On problems NOTIFY, HALT, and CONSULT

Verifications:
- Verify feature name (if provided)
- Verify GitHub issue is suitable as feature item (if provided)
- Verify issue state is "open" (if provided)
- Verify on develop branch
- Verify working tree is clean
- Verify everything is pushed
- Verify feature branch does not exist on remote

Steps:
- Get feature name and GitHub issue (if not provided)
- Parse issue for checkbox steps (if provided)
- Update projmng.yaml with feature.item and active_item
- Advance workflow stage to "in progress"
- Create or checkout feature branch
- Update projmng.yaml with project and feature details

Results:
- Feature branch created or checked out
- projmng.yaml updated with project and feature details
- Workflow stage set to "in progress"
- steps.md populated with feature checkbox steps (if any)

Error handling:
- On error: NOTIFY, HALT, and CONSULT according to halt.dyn.md

# General instructions for this dynamAI task:
Reminders:
- See ai/instructions.md section 2.2. Specifically:
  - Discuss with the developer includes waiting for their response.
  - Stop execution if something is wrong.
  - On error NOTIFY, HALT, and CONSULT according to `halt.dyn.md`

- See formats/format.steps.md for instructions about writing to steps.md file

- See formats/format.dynamAI.md for instructions to close a step
(steps.md, notify developer)

# Steps:

## Input Validation steps

### Step 1.0 Record self
1.0.1 **Record dynamAI task:** write to steps.md:
`## Running dynamAI task: 1a.StartFeature by AI assistant (Cascade)`
No result checking here. Continue.

1.0.2 Write project info to steps.md (if available):
`Project info: {projman.name}, feature/{featurename}`
No result checking here. Continue.

### Step 1.1 First confirmation (feature start)
See dynamAI/confirmAction.dyn.md for CRITICAL HALT INSTRUCTION and confirmation format.

1.1.1 **Display confirmation question** (see format.dynamAI.md "Confirmation question format"):
  - {accomplished}: "Previous feature closed"
  - {assurance}: "Will reconfirm after accomplishment details"
  - {question}: "Proceed with creating new feature branch? {featurename}"
1.1.2 **Follow confirmAction.dyn.md CRITICAL HALT INSTRUCTION** - wait for explicit user approval
On result:
- follow developer's instructions. DO NOT PROCEED WITHOUT THEM

Note: If no featurename given ask for it. 

1.1.2 If GitHub issue URL/number not provided, ask developer for it
1.1.3 If GitHub issue provided:
  - Parse to get: issue number, issue URL, issue title
  - Verify issue is suitable as feature item using aiCode/isFeatureItem.md
  - Verify issue state is "open" using aiCode/itemState.md
  - Parse item body and comments for checkbox steps
  - List all checkbox steps found
  - Update projmng.yaml with feature.item and active_item (see projmngUpdate.dyn.md "On Start Feature" subsection 1)
  - Fetch and cache project details (including statusFieldUID and stage option UIDs) using aiCode/projectDetails.md (see projmngUpdate.dyn.md "On Start Feature" subsection 3)
  - Advance workflow stage to "in progress" using aiCode/itemStage.md setter
On result:
- problem: Notify and follow developer's instructions
- no problem: Follow developer's instructions


### Step 1.2 Read projmng.yaml
1.2.1 Read `dev/ai/working/projmng.yaml` for previous feature and branch context
1.2.2 Warn developer if previous feature branch was not closed
1.2.3 Warn developer if active item still open from previous feature
On result:
- problem: Consult the developer
- no problem: Close step

### Step 1.3 Check local state
1.3.1 Get current local branch: `git branch --show-current`
1.3.2 Handle branch status:
  - If already on `feature/<featurename>`: warn developer, skip to Step 2.3
  - If on another feature branch: warn, ask whether to close it or ignore
  - If not on develop but not a feature branch: warn and stop
1.3.3 Check working tree is clean: `git status --short`
1.3.4 If uncommitted files exist:
  - If only steps.md and projmng.yaml are uncommitted:
    - Check if they reflect a recent completed commit (compare with projmng.yaml commits section)
    - If yes: Notify developer that metadata files are uncommitted but reflect recent commit
    - Consult developer: Should we add a step to commit metadata files only, then re-run dynamAI task?
    - Wait for developer's ok
    - If developer agrees: Add step to steps.md to commit metadata, stop dynamAI task
  - If other files are uncommitted: WARN developer and ask to resolve first
1.3.5 Check everything is pushed: `git status -sb`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 1.4 Check remote
1.4.1 Check if feature branch exists on remote:
  `gh api repos/{project.owner}/{project.repo}/git/refs/heads/feature/<featurename>`
1.4.2 Handle result:
  - 404 = does not exist. Safe to create
  - 200 = already exists. Warn developer and ask how to proceed
On result:
- problem: Consult the developer
- no problem: Close step

## Action steps

### Step 2.1 Critical: Create or move to feature branch
2.1.1 If remote exists and developer confirmed to use it:
  `git checkout feature/<featurename>`
2.1.2 If does not exist:
  `git checkout -b feature/<featurename>`
2.1.3 Verify: `git branch --show-current`
On result:
- problem: Consult the developer
- no problem: Close step

### Step 2.2 Update projmng.yaml and steps.md
2.2.1 Update projmng.yaml (see projmngUpdate.dyn.md "On Start Feature" subsection 5)
2.2.2 If checkbox steps found, populate steps.md with them
On result:
- problem: Consult the developer
- no problem: Close step
