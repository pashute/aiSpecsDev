Document name: 2b.closeProjItem
Version: 0.91

Project development data at current stage

Please complete this

pseudo: 

General:  
- report and stop if anything is missing or goes wrong. 
- if you find a way to continue, don't. Instead, suggest it to the developer and wait for ok. 

step:  steps.md 
- write to steps.md the dynamAI name (2b.CloseProjdevItem) and the following info: 
- proj branch itemnum itemname with link to url. 
- update during the steps if there were any changes decided with the developer.
         
step: re-confirm with user item number and title to close

step: Check current item (in projdev.yaml) is the one requested to close
- Verify active_item.id matches requested item number
- If mismatch, warn developer and stop

step: check in steps.md that same item is listed, and that all steps have been completed.
- Verify all steps marked [v] (done)
- If incomplete steps exist, warn developer and ask to complete first

step: check all committed and pushed
- warn developer if not and ask how to proceed

step: Verify no unclosed subtasks exist
- Check active_item.subtasks array in projdev.yaml
- For each subtask, verify state is "closed"
- If any subtask is open, warn developer and ask to close subtasks first


step: Make item closure note. See template. Take info from projdev.yaml

step: create the closing multiline markdown comment: 

step: move item from in-progress to done workflow-stage (Github Project V2 item ) 

step: make closing comment in following markdown format:

```markdown
[{itemNum}]({itemUrl}):{itemTitle}
steps:
- [ ] {stepNum}. {stepTitle} 
  commits: 
  - [{commitHash}]({commitUrl}): {commitHeadline}
    - files: [{file1}!]({fileurl}), [{file2}]({fileurl}), [{file3}]({fileurl}) # ! after filename if important change
```

step: put comment in multiline powershell here-string:
-- enclosed in @' '@ 
-- with open at end of line, no trailing spaces
-- close at beginning of line, no preceding spaces or text, no trailing spaces or text. 

step: decide if completed / duplicate / "not planned"
- ask user which.
- in case of "not planned" add the quotemarks in the command's reason. 
- in case of duplicate find and suggest or ask for duplicate item number

step: Close item with reason and comment using the following gh calls

```
# for completed / "not planned":
gh issue close {itemnum} --repo {owner}/{repo} --reason {reason} --comment $comment

# for duplicate:
gh issue close {itemnum} --repo "$owner/$repo" --reason duplicate --duplicate-of {dupItemnum} --comment $comment
```

step: Move to workflow-stage (GH ProjV2 Item status) DONE



i.e. 


- check if this item has a parent (feature.item or subitem parent).
to check if all closed. if one isn't propose to go to it next and finish it.
If all subtasks are closed, propose to close parent task.
This should be done recursively till all line is closed. 